
```bash
# Access RPC service anonymously
rpcclient -U "" <IP>
```

```bash
# Get server OS and version info
srvinfo

# List domains in network
enumdomains

# Get domain details
querydominfo

# List all shares
netshareenumall

# Get detailed info about a share
netsharegetinfo <share>

# List domain users
enumdomusers

# Get user info by RID
queryuser <RID>
```

### RID Bruteforce

```bash
# Bruteforce user RIDs to enumerate users
for i in $(seq 500 1100); do rpcclient -N -U "" <IP> -c "queryuser 0x$(printf '%x\n' $i)"; done

# Automated user enumeration (alternative)
samrdump.py <IP>
```