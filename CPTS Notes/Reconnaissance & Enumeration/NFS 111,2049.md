
```bash
# List available NFS shares
showmount -e <IP>

# Mount NFS share locally
mount -t nfs <IP>:/share ./mnt -o nolock

# Unmount NFS share
umount ./mnt

# Scan NFS services and exports
nmap --script nfs* -p111,2049 -sV <IP>
```